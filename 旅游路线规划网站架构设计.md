# 旅游路线规划网站 - 项目架构与开发计划

本文档旨在为旅游路线规划网站项目提供一套完整的技术架构设计和详细的实施路线图。

## 1. 项目愿景与核心功能

**愿景**: 创建一个智能、直观、个性化的旅游路线规划工具，结合大语言模型（LLM）的智能规划能力与高德地图的精确地理信息，让用户可以轻松地一键生成精确到时间点和地点的详细行程，并通过可视化的地图提升旅行规划的效率和乐趣。

**核心功能**:

- **用户系统**: 用户注册、登录、个人信息管理（基于 JWT）。
- **AI 智能生成行程**: 用户输入出发地、目的地、天数、预算、交通偏好及必去景点，系统调用大语言模型（如 DeepSeek/MiniMax）自动生成结构化的每日详细行程。
- **行程创建与管理 (CRUD)**: 创建、查看、重新规划、删除一个完整的旅行计划。
- **地图可视化与交互**: 在高德地图上实时显示行程路线、各个地点标记。点击时间轴景点，地图自动拉近并高亮展示对应坐标。
- **智能路线与交通计算**: 调用高德地图 API 自动补全景点经纬度，并规划相邻景点之间的实际驾车/步行路线及预估耗时。
- **社交平台攻略整合**: LLM 在生成行程时，可通过 Function Calling 调用后端工具，自动搜索小红书/抖音的最新避坑指南和美食推荐并融入行程。
- **分享功能**: 支持将规划好的行程生成二维码分享。

## 2. 系统架构设计 (Enterprise-Level)

采用“前后端分离”架构，核心业务逻辑高度依赖大模型与第三方地图 API。

### 2.1. 技术选型与说明

- **前端 (Frontend)**
  - **框架**: `React.js` (v18+) + `React Router`。
  - **UI 库**: `Ant Design` (v5)。提供高质量的企业级组件（App 上下文、卡片、时间轴等）。
  - **地图集成**: `@amap/amap-jsapi-loader`。封装自定义 `AMap` 组件，处理标记点、折线绘制与视图自适应。
  - **HTTP请求**: `Axios`，带有请求拦截器（附带 JWT Token）。
- **后端 (Backend)**
  - **框架**: `NestJS` (基于 TypeScript)。提供模块化、依赖注入，适合构建高可维护性的后端服务。
  - **数据库 ORM**: `Prisma`。类型安全的 ORM，操作 MySQL。
  - **核心模块**:
    1. **`AuthModule`**: 用户认证与 JWT 签发。
    2. **`ItineraryModule`**: 核心业务，负责行程与每日计划（PlanItem）的存取，协调 LLM 与地图服务。
    3. **`LlmService`**: 封装 OpenAI SDK，对接国产大模型（DeepSeek / MiniMax），处理复杂的 Function Calling、XML/JSON 文本清洗与重试容错机制。
    4. **`AmapService`**: 封装高德地图 Web API（v3/v5），负责 POI 搜索（经纬度解析）、路径规划、天气查询。
- **数据库 (Database)**
  - **类型**: `MySQL 8.0+`。
- **部署环境**
  - **包管理**: `npm`。
  - **环境变量**: `.env` 集中管理数据库连接串、JWT 密钥、高德 API Key 及 LLM API Key。

### 2.2. 数据库核心表结构设计 (MySQL / Prisma Schema)

- **User**: 用户基本信息与凭证。
- **Itinerary**: 行程主表，记录出发地、目的地、预算、起止时间等元数据。
- **PlanItem**: 行程明细表，记录具体的景点名称、经纬度、到达时间、停留时长、预估花费、交通方式及 AI 生成的描述（含避坑指南）。关联至特定的 `Itinerary`。

*(注：实际表结构以 `backend/prisma/schema.prisma` 为准。)*

------

## 3. 核心业务流程 (AI 行程生成)

1. **用户输入**: 前端提交表单（目的地、预算、天数等）。
2. **LLM 规划**: 后端 `LlmService` 构建 Prompt 唤起大模型。大模型在思考过程中调用 `searchSocialMedia` 抓取最新攻略。
3. **数据清洗**: 后端对 LLM 返回的文本进行正则清洗，剥离 `<think>`、`<invoke>` 等 XML 标签，提取纯净的 JSON。
4. **地理编码**: 遍历 JSON 中的地点，调用 `AmapService.search` 在全国范围内检索经纬度坐标。
5. **数据持久化**: 使用 Prisma 开启事务，将行程和附带经纬度的计划项写入 MySQL。
6. **前端渲染**: 前端拉取行程数据，左侧渲染 Timeline，右侧渲染 AMap，并按天调用后端路由规划接口绘制路径折线。

------

## 4. 开发阶段规划 (已完成情况)

### **第一阶段：基础设施与认证 (✅ 已完成)**
- 初始化 React 与 NestJS 项目。
- Prisma Schema 设计与 MySQL 连接。
- 实现 JWT 注册登录流程。

### **第二阶段：地图服务与核心 CRUD (✅ 已完成)**
- 封装高德地图 API（POI 搜索、路径规划）。
- 前端地图组件 `AMap.js` 的封装。
- 基础行程增删改查。

### **第三阶段：AI 智能化与稳定性优化 (✅ 已完成)**
- 引入 `LlmService` 并对接大模型。
- **稳定性攻坚**: 解决国产大模型 Function Calling 幻觉，处理嵌套括号的 XML 标签清洗，修复 JSON 解析崩溃问题。
- **定位优化**: 解决高德 API 在受限城市内搜不到大型景区导致定位漂移至非洲（0,0）的问题。

### **第四阶段：交互体验优化 (✅ 已完成)**
- 实现前端左侧卡片与右侧地图的点击联动（平滑缩放、图标高亮）。
- 时间轴支持全局折叠/展开，优化长标题防挤压显示。
- 行程“重新规划”功能的就地更新。

### **第五阶段：进阶与部署 (🚀 待进行/未来规划)**
- **数据导出**: 支持将行程导出为 PDF 或长图。
- **多交通混合优化**: 针对同一天内的跨城与市内交通做更精细的路径预估。
- **生产环境部署**: Docker 容器化构建，配置 Nginx 反向代理与 CI/CD 流水线。